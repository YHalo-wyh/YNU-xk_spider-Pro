# YNU选课助手 Pro v2.7.0

## 批次安全与查询诊断修复版本

本次版本聚焦“选课批次切换后的待选课程安全、监控查询可诊断性、会话过期恢复和更新检查稳定性”，解决旧教学班 ID 被带入新批次后持续查询失败、运行日志缺少明确原因以及 GitHub API 403 导致无法检查更新的问题。

---

## 核心更新

### 1. 待选课程与选课批次绑定

- 待选状态现在会保存自动识别到的 `batch_code` 和批次名称。
- 登录后仅恢复与当前选课批次一致的教学班。
- 检测到上一批次保存的课程时，程序会清空旧待选列表并提示从当前批次重新添加。
- v2.6.0 及更早版本保存的状态没有批次信息，升级后首次登录不会直接恢复其中的旧 `JXBID`。

### 2. 监控查询失败原因细分

- 当前批次查询结果为空时记录 `empty_result`。
- 查询结果中没有目标教学班时记录 `teaching_class_not_found`。
- 服务器非 200 响应记录 `http_error`，接口拒绝记录 `api_rejected`。
- 网络超时、请求异常和返回格式异常会分别记录对应原因。
- 日志包含课程名、教学班号、当前批次和失败类型，不记录密码或 Token。

### 3. 恢复每门待选课程的监控状态日志

- 成功查询时继续显示每门课程的余量、容量及是否满额。
- 查询失败时显示具体课程和失败原因，不再只输出笼统的“查询失败”。
- 同一课程、同一失败原因每 60 秒最多输出一次，避免长期运行时日志刷屏和界面变卡。
- 查询失败只跳过当前循环，不会在无法确认余量时发送选课请求。

### 4. HTTP 401/403 会话过期识别

- 会话检测现在同时识别 HTTP `302`、`401` 和 `403`。
- 收到未授权响应后触发自动重登，恢复成功后重试当前请求。
- 自动重登失败时通知界面并停止相关监控，避免把授权问题误报为普通课程查询失败。

### 5. GitHub 更新检查 403 回退

- 检查更新仍优先使用 GitHub Releases API。
- API 因匿名请求限额返回 403 或其他异常状态时，自动访问公开的 Latest Release 页面识别最新标签。
- 找到安装包时继续提供直接下载；无法确认安装包时打开对应 Release 页面。

### 6. 版本与构建同步升级

- 程序版本升级至 `v2.7.0`。
- 安装包和便携包文件名同步更新为 `v2.7.0`。
- 发布包继续包含主程序、Watchdog 和隔离的 OCRHelperRuntime。

---

## 兼容性说明

- 升级不会把上一批次的教学班 ID 自动带入当前批次。
- 旧版待选状态因缺少批次信息，需要在当前批次课程列表中重新添加一次。
- 如果用户手动保留了当前批次不存在的教学班，监控会记录 `empty_result` 或 `teaching_class_not_found`，并安全跳过本轮请求。
- 当前批次不存在的课程不会触发选课提交，也不会导致程序崩溃。

---

## 发布文件

| 文件 | 说明 | SHA-256 |
| --- | --- | --- |
| `YNU.Pro_v2.7.0_Setup.exe` | 推荐使用的安装版，支持覆盖安装 | `711F7069884C96D4BA4B642EBECDC72E4D30F40FD637C9ABB8AC80BD9A25BFBA` |
| `YNU.Pro_v2.7.0_Portable.zip` | 便携版，解压后运行 | `DBBAAC70D41DD24E8E6E8C0D6271AD6476A0DE11C394EA9D4CDBB17BDD85A759` |

---

## 升级建议

1. 正在监控时先点击“停止”，然后关闭旧版本主程序和 Watchdog。
2. 推荐使用 `YNU.Pro_v2.7.0_Setup.exe` 覆盖安装，也可以将便携版解压到新目录。
3. 首次登录后确认程序识别到的选课批次，并重新添加因批次不一致而被清除的待选课程。
4. 开始监控后若出现 `teaching_class_not_found`，请移除该课程并从当前批次查询结果中重新添加。

---

## 注意事项

- 课程是否存在、是否开放以及余量信息均以云南大学选课系统当前批次的实时返回为准。
- `empty_result` 表示本次查询没有返回课程数据；`teaching_class_not_found` 表示返回了课程数据，但其中没有待选列表保存的目标教学班。
- GitHub API 403 通常由匿名请求限额或网络出口共享限额引起，本版本已增加公开发布页回退；若 GitHub 整体不可访问，仍需通过浏览器手动打开 Release 页面。
- 安装包体积主要来自独立 OCR 运行时及验证码识别模型，用于隔离 ONNX Runtime 与主程序 PyQt 依赖，避免验证码组件因 DLL 冲突无法启动。

---

感谢大家的反馈与测试支持。
