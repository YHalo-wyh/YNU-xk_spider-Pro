# YNU选课助手 Pro v2.7.0

v2.7.0 修复选课批次切换后恢复旧待选课程导致持续查询失败的问题，并完善课程监控日志、会话过期识别、检查更新功能和 OCR 运行时打包。

本次为 v2.7.0 的增量稳定性更新，不改变版本号和原有功能入口。

## 本次更新

### 增量稳定性修复

- 多门课程同时检测到 Session 失效时，自动重登只允许单条恢复链路运行，不再重复创建登录线程。
- 修复并发重登等待逻辑，不再用旧 Token 是否存在误判重登成功。
- 自动重登产生的临时 HTTP Session 会在每次尝试结束后及时关闭，减少长时间运行后的资源累积。
- 普通限流、WAF 或接口拒绝产生的 HTTP `403` 不再直接触发自动重登；只有响应明确表示认证失效时才会恢复会话。
- 修复长时间监控后线程 Session 可能同时发送旧、新 `JSESSIONID` 的问题，每次请求只携带当前认证代际的 Cookie。
- 新增 Token 代际校验，旧 Token 请求晚到的 `401` 不再使新登录结果失效。
- 单次、无明确失效内容的 `401` 先由下一次受保护请求复核；连续两次失败后再使用独立的已选课程接口交叉复核。
- 任何成功的受保护请求都会清除 `401` 连续失败计数，避免单个接口的瞬时异常引发重登风暴。
- 监控停止超时后不会立即创建新的 worker，避免旧监控线程与新线程并行运行。
- 旧监控 worker 的延迟信号不会再更新当前 UI，降低 Qt 控件生命周期异常和闪退风险。
- 关闭窗口时会等待登录与监控线程自然退出，避免后台线程仍运行时销毁 Qt 对象。
- 崩溃日志现在记录完整 Python traceback，便于定位后续异常。

### 选课批次识别优化

- 待选状态现在会保存程序自动识别到的选课批次代码和批次名称。
- 登录成功后会核对已保存课程与当前选课批次是否一致。
- 检测到上一批次保存的课程时，不再将旧教学班 ID 恢复到当前待选列表。
- 跨批次课程会被安全清除，并提示从当前批次课程列表重新添加。
- v2.6.0 及更早版本保存的待选状态没有批次信息，升级后首次登录需要重新添加待选课程。
- 避免旧教学班 ID 被用于查询新批次课程，减少持续查询失败和无效请求。

### 课程监控日志优化

- 恢复每门待选课程的监控状态日志。
- 查询成功时显示课程名称、剩余名额、课程容量和满额状态。
- 查询失败时显示具体课程和失败原因，不再只记录笼统的“查询失败”。
- 当前批次没有返回课程数据时记录 `empty_result`。
- 查询结果中没有目标教学班时记录 `teaching_class_not_found`。
- 服务器返回异常状态时记录 `http_error` 或 `api_rejected`。
- 网络超时、请求异常和数据格式异常会分别记录对应错误类型。
- 相同课程和相同失败原因每 60 秒最多记录一次，避免日志持续刷屏。
- 减少长时间运行后日志过多造成的界面卡顿和监控效率下降。

### 监控安全优化

- 无法确认课程余量时，只跳过当前查询循环。
- 查询失败时不会直接发送选课请求。
- 当前批次不存在的课程不会触发抢课、换课或退课操作。
- 如果课程被手动保留但不属于当前批次，监控会记录 `empty_result` 或 `teaching_class_not_found`。
- 发现无效教学班后，建议将课程从待选列表移除，并从当前批次查询结果中重新添加。

### 会话过期识别修复

- 会话检测现在同时识别 HTTP `302`、`401` 和 `403`。
- `302`、明确的认证失效内容仍会直接触发恢复；普通 `403` 不会触发重登。
- 单次 `401` 会先复核；连续两次 `401` 且已选课程接口交叉复核也失败时，才确认 Session 过期。
- 重登前已发出的旧 Token 请求，其延迟返回的 `401` 会被忽略并使用当前会话重试。
- 自动重登成功后会重新尝试当前请求。
- 自动重登失败时会通知界面并停止相关监控。
- 避免将登录状态失效错误显示成普通课程查询失败。

### 检查更新修复

- 检查更新仍优先通过 GitHub Releases API 获取最新版本。
- GitHub API 因匿名请求限额返回 HTTP 403 时，会自动切换到公开的 Latest Release 页面。
- 程序会从 Latest Release 跳转地址中识别最新版本标签。
- 找到对应安装包时继续提供直接下载。
- 无法确认安装包时会打开对应的 GitHub Release 页面。
- 检查更新失败弹窗保留重试和手动打开发布页面入口。

### OCR 运行时优化

- 保留 OCR 隔离进程，避免 ONNX Runtime 与主程序 Qt 依赖发生 DLL 冲突。
- OCR 运行时仅打包验证码分类所需的 `common_old.onnx`、字符集和必要依赖。
- 移除未使用的检测模型、旧模型副本、滑块识别、API 和 OpenCV 内容，缩小发布包体积。
- 修复发布包中的 `OCRHelper.exe` 缺少 `onnxruntime` Python 包、导致登录时验证码始终识别失败的问题。
- 修复精简 OCR Helper 漏掉 `[-1, 1]` 灰度归一化、导致验证码识别率异常的问题。
- 打包完成后会实际启动 OCR Helper 并加载 ONNX 模型；自检未收到 `READY` 握手时禁止生成发布包。

## 安全说明

- 跨批次保存的旧教学班不会自动恢复到当前待选列表。
- 当前批次不存在的课程不会触发选课提交。
- 查询失败时程序会采用安全跳过策略，不会在余量未知时盲目抢课。
- `empty_result` 表示本次查询没有返回课程数据。
- `teaching_class_not_found` 表示查询返回了课程数据，但其中没有待选列表保存的目标教学班。
- 自动换课仍存在先退旧课程、再选择新课程的时间窗口，请根据课程重要程度谨慎使用。
- 网络异常、名额变化或多人同时抢课时，无法保证选课和回滚一定成功。

## 下载

- `YNU.Pro_v2.7.0_Setup.exe`：安装版，SHA-256：`F9D73EBE3C6B5ADA4D5F8B0BE7C1462148CB9B53E16FD5037D401305224B6899`
- `YNU.Pro_v2.7.0_Portable.zip`：便携版，SHA-256：`BB57F527E2F43339C9E0A3A4A346FBB5F314313A670F1A31993EA9EB14C22A6F`

安装版默认安装到当前用户目录，不需要管理员权限。账号配置、待选课程、监控策略和日志保存在独立用户数据目录，覆盖安装不会主动删除这些数据。

升级后首次登录时，如果程序提示旧待选课程没有批次信息，请从当前选课批次的课程查询结果中重新添加。

## 注意事项

本工具仅供学习交流，请遵守学校相关规定并合理设置监控频率。

课程是否存在、是否开放以及剩余名额均以云南大学选课系统当前批次的实时返回结果为准。

本项目所有功能和发布文件均免费提供，严禁任何单位或个人以任何形式收费。
