# YNU选课助手 Pro v2.7.0

## 批次安全与监控诊断修复

本次版本修复选课轮次切换后恢复旧待选课程导致持续“查询失败”的问题，同时完善会话过期识别、查询错误日志和 GitHub 更新检查回退。登录、验证码识别、课程浏览、选课提交、冲突换课、回滚及 Watchdog 原有流程保持不变。

## 核心更新

### 1. 待选课程绑定选课批次

- 新保存的待选状态会记录自动识别到的 `batch_code` 和批次名称。
- 登录后只恢复与当前批次一致的教学班。
- 检测到上一轮保存的待选课程时，程序会在开始监控前安全清除，不再使用旧 `JXBID` 请求当前批次。
- v2.6.0 及更早版本的状态文件没有批次信息，升级后首次登录会提示重新从当前批次添加课程。

### 2. 当前批次不存在课程时给出明确原因

- 当前批次返回了课程数据，但找不到目标教学班：显示“当前批次未找到教学班 (`teaching_class_not_found`)”。
- 当前批次查询结果为空：显示“当前批次查询结果为空 (`empty_result`)”。
- 查询失败时继续采用安全模式，只跳过本轮查询，不会盲抢或发送选课请求。
- 相同错误按课程每 60 秒最多输出一次，避免日志刷屏和界面长期运行变卡。

### 3. 修复 HTTP 401/403 会话恢复

- 监控查询、选课和退课共用的会话检测现在识别 `302`、`401` 和 `403`。
- 收到未授权响应后触发原有自动重登流程，恢复成功后立即重试一次当前请求。
- 自动重登失败时停止相关监控并通知界面，不再把授权错误伪装成普通查询失败。

### 4. 查询失败日志可诊断

运行日志现在区分：

- `http_error`：服务器返回非 200 状态
- `api_rejected`：接口返回失败状态或说明
- `empty_result`：当前查询结果为空
- `teaching_class_not_found`：结果内没有目标教学班
- `timeout` / `request_error`：超时或网络请求异常
- `invalid_json` / `invalid_data_list`：服务端返回格式异常
- `unexpected_error`：其他程序解析异常

日志会记录课程名、教学班号、当前批次及失败类型，不记录密码和 Token。

### 5. GitHub 更新检查 403 回退

- 仍优先使用 GitHub Releases API 获取版本和安装包。
- API 因匿名限额返回 403 等状态时，自动访问公开的 Latest Release 跳转识别最新标签。
- 确认安装包存在时继续提供直接下载；否则打开对应 Release 页面。

## 发布文件

| 文件 | 说明 |
| --- | --- |
| `YNU.Pro_v2.7.0_Setup.exe` | 推荐使用的安装版，可覆盖安装 |
| `YNU.Pro_v2.7.0_Portable.zip` | 便携版，解压后运行 |

## 升级说明

1. 关闭旧版本；正在监控时请先点击“停止”。
2. 使用 `YNU.Pro_v2.7.0_Setup.exe` 覆盖安装，账号配置仍保存在用户数据目录。
3. 升级后首次登录若提示旧待选课程没有批次信息，请在当前批次课程列表中重新添加。
4. 开始监控后若出现 `teaching_class_not_found`，说明该教学班不在当前批次查询结果中，应移除后重新搜索当前批次课程。

## 完整性校验

发布附件同时提供 SHA-256 校验值。下载后可在 PowerShell 中执行：

| 文件 | SHA-256 |
| --- | --- |
| `YNU.Pro_v2.7.0_Setup.exe` | `711F7069884C96D4BA4B642EBECDC72E4D30F40FD637C9ABB8AC80BD9A25BFBA` |
| `YNU.Pro_v2.7.0_Portable.zip` | `DBBAAC70D41DD24E8E6E8C0D6271AD6476A0DE11C394EA9D4CDBB17BDD85A759` |

```powershell
Get-FileHash .\YNU.Pro_v2.7.0_Setup.exe -Algorithm SHA256
Get-FileHash .\YNU.Pro_v2.7.0_Portable.zip -Algorithm SHA256
```
