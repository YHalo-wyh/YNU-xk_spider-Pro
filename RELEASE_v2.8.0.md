# YNU选课助手 Pro v2.8.0

v2.8.0 修复课程已全部处理但界面待抢列表仍有残留、权威查询确认已选后没有成功弹窗的问题，并进一步降低瞬时 `401` 导致 Session 误判过期和重复重登的概率。

## 本次更新

### 待抢列表状态同步

- worker 内部课程全部处理完毕时，会发送专用完成信号同步界面状态。
- 界面待抢列表、待抢数量、冲突策略和自动恢复状态会一并清理。
- Watchdog 会收到停止信号，重启程序后不会再次恢复已经处理完毕的课程。
- 旧 worker 的晚到完成信号仍受身份校验保护，不会清空新 worker 的待抢列表。

### 选课成功反馈

- 修复选课提交曾返回“容量已满”，但后续权威查询已经显示 `isChoose=True` 时静默结束的问题。
- 权威查询确认课程已选后，会正常显示成功弹窗。
- 同时记录 `[SUCCESS]` 日志，并发送已经配置的 Server酱或 Webhook 成功通知。

### Session 稳定性

- 每次受保护请求都会清理线程 Session 中的旧 Cookie，只携带当前认证代际的 `JSESSIONID`。
- 重登前发出的旧 Token 请求，即使延迟返回 `401`，也不会再使新会话失效。
- 单次且没有明确登录失效内容的 `401` 不再立即触发自动重登。
- 连续两次受保护请求失败后，会再通过独立的已选课程接口交叉复核；只有复核也失败时才确认 Session 过期。
- 任意成功的受保护请求都会清除连续失败计数，降低接口瞬时异常导致的重登风暴。
- 普通限流、WAF 或接口拒绝产生的 `403` 仍不会被误判为 Session 过期。

### 界面提示

- 待抢列表为空时点击“开始选课”，提示内容改为居中显示。
- 提示文字调整为“待抢列表为空，请先添加待抢课程”，原因更明确。

### 兼容性说明

- 课程监控轮询间隔保持原值，本次没有降低监控频率。
- 原有登录、课程查询、批次识别、多课程监控、自动重登、冲突组策略、自动换课、Server酱/Webhook、Watchdog、课表和更新检查功能均保留。
- 覆盖安装会保留账号配置和用户状态；课程全部处理完毕后，已完成的待抢状态会被安全清空。

## 下载

- `YNU.Pro_v2.8.0_Setup.exe`：安装版，SHA-256：`219B30DD46ED720395331C06C56E61F2894FFBF1E64B67CF04C14E2ECCD45A15`
- `YNU.Pro_v2.8.0_Portable.zip`：便携版，SHA-256：`5D0116C5536F83C036FA2248FB4FB13F516C41C36CDE1C19EDC68CE370C78057`

安装版默认安装到当前用户目录，不需要管理员权限。便携版解压后直接运行 `YNU选课助手Pro.exe`。

## 注意事项

本工具仅供学习交流，请遵守学校相关规定并合理设置监控频率。

课程是否存在、是否开放以及剩余名额均以云南大学选课系统当前批次的实时返回结果为准。网络异常、名额变化或多人同时抢课时，无法保证选课和回滚一定成功。

本项目所有功能和发布文件均免费提供，严禁任何单位或个人以任何形式收费。
